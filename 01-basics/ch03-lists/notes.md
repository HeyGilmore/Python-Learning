# Python Learning Journal

## Chapter 3 - Introducing Lists

### Date 
June 19, 2026

## What I Learned

- What a list is and why it is useful.
- How to create a list.
- How to access items in a list using indexes.
- Python list indexes start at `0`, not `1`.
- How to modify existing elements in a list.
- How to add new items to a list.
- How to remove items from a list.
- How to organize and sort lists.
- How to determine the length of a list.
- How index errors occur and how to avoid them.

## Programs Created

- bicycles.py
- sports.py

## Challenges I Faced

- Remembering that list indexes start at 0.
- Knowing when to use `pop()` versus `remove()`.
- Understanding the difference between `sort()` and `sorted()`.


## Most Important Takeaways

1. Lists store multiple values in one variable.
2. Indexes start at 0.
3. `append()` adds items.
4. `pop()` removes and returns items.
5. `remove()` deletes by value.
6. `sort()` changes the list permanently.
7. `sorted()` creates a temporary sorted view.
8. `len()` returns the number of items.
9. Index errors occur when accessing positions that don't exist.
10. Lists are one of the most important data structures in Python.


## Most Important Takeaways

1. Lists store multiple values in one variable.
2. Indexes start at 0.
3. `append()` adds items.
4. `pop()` removes and returns items.
5. `remove()` deletes by value.
6. `sort()` changes the list permanently.
7. `sorted()` creates a temporary sorted view.
8. `len()` returns the number of items.
9. Index errors occur when accessing positions that don't exist.
10. Lists are one of the most important data structures in Python.
